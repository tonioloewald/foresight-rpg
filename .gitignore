# macOS
.DS_Store
**/.DS_Store

# Legacy source materials — large binaries / originals, kept out of the public repo
legacy/
Abortive Previous Attempt/
*.afphoto
*.key

# scratch
*.tmp

# build artifact emitted at repo root (the served copy is docs/llms.txt)
/llms.txt

# build / deps
node_modules/
dist/
demo/
bun.lock
tls/

# generated JS source maps in the served site — never tracked (debug artifacts,
# and hydrate.js.map alone is >1MB). The .js they map IS committed.
docs/**/*.map

# Large derived asset — regenerate from legacy/ForeSight.pdf via gs (see legacy/foresight-1986/); the served copy in docs/ is committed for Pages.
static/foresight-1986.pdf
